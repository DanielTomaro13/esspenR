% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_soccer_transfers.R
\name{fetch_all_football_transfers}
\alias{fetch_all_football_transfers}
\title{Fetch all football transfers with pagination}
\usage{
fetch_all_football_transfers(
  league,
  batch_size = 1000,
  max_transfers = 10000,
  raw = FALSE
)
}
\arguments{
\item{league}{Character. League identifier (e.g., "eng.1", "usa.1", "esp.1").}

\item{batch_size}{Integer. Number of transfers to fetch per request (default: 1000).}

\item{max_transfers}{Integer. Maximum total transfers to fetch (default: 10000).}

\item{raw}{Logical. If TRUE, assigns raw JSON to global environment. Default FALSE.}
}
\value{
Invisibly returns the data, but main purpose is global environment assignment.
}
\description{
Fetch all football transfers with pagination
}
\examples{
\dontrun{
# Get all Premier League transfers
fetch_all_football_transfers("eng.1")

# Get all MLS transfers with smaller batches
fetch_all_football_transfers("usa.1", batch_size = 500)

# Get all La Liga transfers
fetch_all_football_transfers("esp.1")
}

}
