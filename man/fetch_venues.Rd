% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_venues.R
\name{fetch_venues}
\alias{fetch_venues}
\title{Fetch ESPN League Venue Details}
\usage{
fetch_venues(sport, league, limit = 1000, raw = FALSE, progress = TRUE)
}
\arguments{
\item{sport}{Character. Sport slug (e.g. 'football', 'basketball').}

\item{league}{Character. League slug (e.g. 'nfl', 'nba').}

\item{limit}{Integer. Maximum number of venues to fetch (default: 1000).}

\item{raw}{Logical, default FALSE. If TRUE, returns full parsed JSON data.}

\item{progress}{Logical, default TRUE. Show progress bar for venue fetching.}
}
\value{
A tibble with venue id, name, capacity, city, state or raw JSON if \code{raw = TRUE}.
}
\description{
Lists all venues for a given sport and league using ESPN's sports.core.api,
fetching full details for each venue (name, capacity, city, state).
}
\examples{
fetch_venues("football", "nfl")
fetch_venues("basketball", "nba", raw = TRUE)
fetch_venues("football", "nfl", limit = 50, progress = FALSE)
}
