% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_soccer_transfers.R
\name{fetch_football_transfers}
\alias{fetch_football_transfers}
\title{Fetch football (soccer) transfer data using ESPN API}
\usage{
fetch_football_transfers(league, limit = 100, offset = 0, raw = FALSE)
}
\arguments{
\item{league}{Character. League identifier (e.g., "eng.1", "usa.1", "esp.1", "ger.1", "ita.1", "fra.1").}

\item{limit}{Integer. Number of transfers to fetch (default: 100, max: 1000).}

\item{offset}{Integer. Starting position for pagination (default: 0).}

\item{raw}{Logical. If TRUE, assigns raw JSON to global environment as 'football_transfers_raw'. Default FALSE.}
}
\value{
Invisibly returns the data, but main purpose is global environment assignment.
}
\description{
Fetch football (soccer) transfer data using ESPN API
}
\examples{
\dontrun{
# Get Premier League transfers
fetch_football_transfers("eng.1")
head(football_transfers)

# Get MLS transfers with pagination
fetch_football_transfers("usa.1", limit = 50, offset = 100)

# Get La Liga transfers
fetch_football_transfers("esp.1", limit = 200)

# Get raw data for custom processing
fetch_football_transfers("ger.1", limit = 50, raw = TRUE)
str(football_transfers_raw, max.level = 2)
}

}
