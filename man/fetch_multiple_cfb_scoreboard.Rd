% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_college_football_scoreboard.R
\name{fetch_multiple_cfb_scoreboard}
\alias{fetch_multiple_cfb_scoreboard}
\title{Fetch college football scoreboard for multiple conferences}
\usage{
fetch_multiple_cfb_scoreboard(
  conferences,
  week = NULL,
  year = as.integer(format(Sys.Date(), "\%Y")),
  season_type = 2,
  dates = NULL,
  delay = 0.5,
  raw = FALSE
)
}
\arguments{
\item{conferences}{Character vector. Conference names or group IDs to fetch.
Can use conference names from \code{get_cfb_conference_groups()} or group IDs.
Examples: c("SEC", "Big Ten"), c("8", "5"), c("SEC", "5").}

\item{week}{Integer. Week number to fetch (default: NULL for current week).}

\item{year}{Integer. Season year to fetch (default: current year).}

\item{season_type}{Integer. Season type (default: 2 for regular season).}

\item{dates}{Character. Date in YYYYMMDD format (default: NULL).}

\item{delay}{Numeric. Delay in seconds between API requests (default: 0.5).}

\item{raw}{Logical. If TRUE, assigns raw JSON to global environment
for the first conference only (default: FALSE).}
}
\value{
Invisibly returns the combined data frame. The main purpose is global
environment assignment of combined \code{cfb_scoreboard} from all conferences.
}
\description{
Retrieves scoreboard data for multiple conferences with rate limiting.
This function calls \code{\link{fetch_college_football_scoreboard}} for each conference
and combines the results.
}
\examples{
\dontrun{
# Get Power 5 conferences
fetch_multiple_cfb_scoreboard(c("SEC", "Big Ten", "ACC", "Big 12", "Pac-12"))

# Get specific conferences by ID
fetch_multiple_cfb_scoreboard(c("8", "5"), week = 10)

# Mix names and IDs
fetch_multiple_cfb_scoreboard(c("SEC", "5", "ACC"), week = 12)
}

}
\seealso{
\code{\link{fetch_college_football_scoreboard}} for single conference data
}
